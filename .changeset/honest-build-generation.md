---
"@runfusion/fusion": patch
---

summary: Detect stale CLI builds before startup and ship a component compatibility matrix.
category: fix
dev: Build metadata attests source HEAD, CLI and Dashboard bytes, bundled plugins, and PostgreSQL schema generation; prepack now forces a fresh full-generation build.
